SET(LIB_NAME "${PROJECT_NAME}_core")
ICMAKER_SET(${LIB_NAME})

ICMAKER_ADD_SOURCES(
  demo.cc
)

ICMAKER_ADD_HEADERS(
  demo.h
)

ICMAKER_LOCAL_CPPDEFINES(-D${PROJECT_NAME_UPPER}_EXPORT_SYMBOLS)
ICMAKER_GLOBAL_CPPDEFINES(-D_IC_BUILDER_${PROJECT_NAME_UPPER}_)

ICMAKER_INCLUDE_DIRECTORIES(${${PROJECT_NAME_UPPER}_INCLUDE_DIRS})

ICMAKER_DEPENDENCIES(EXPORT
)

ICMAKER_BUILD_LIBRARY()

ICMAKER_INSTALL_GLOBHEADERS(${LIB_NAME})
